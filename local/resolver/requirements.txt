fastapi==0.115.0
uvicorn==0.30.6
mutagen==1.47.0
# yt-dlp is intentionally unpinned and updated on container start (see
# Dockerfile CMD) so it keeps pace with upstream extractor fixes.
yt-dlp
# yt-dlp plugin that fetches PO tokens from the bgutil provider sidecar, so
# YouTube's "not a bot" check passes without manual cookie refreshes. Keep its
# major version matching the brainicism/bgutil-ytdlp-pot-provider image.
bgutil-ytdlp-pot-provider
# Lets yt-dlp use a SOCKS proxy (YTDLP_PROXY) — e.g. a home reverse-SSH tunnel.
PySocks
# Used by /proxy to relay audio bytes through the same SOCKS proxy (needs the
# [socks] extra, provided by PySocks above) so playback isn't IP-locked.
requests
