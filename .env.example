# Copy to .env and fill in. Never share your real .env.

# Your domain (HTTPS variant only). If using plain HTTP, this can be anything.
DOMAIN=example.com

# Postgres credentials (use a strong password).
DB_NAME=piped
DB_USER=piped
DB_PASSWORD=change_me_to_a_strong_password
