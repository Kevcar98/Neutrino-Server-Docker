# Copy to .env and fill in. Never share your real .env.

# Your domain (HTTPS variant only — used by Caddy for the certificate).
# For the plain HTTP variant you don't need a .env at all.
DOMAIN=example.com
