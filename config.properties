# Piped backend config (HTTPS variant). Replace every <your-domain> with your real domain.
# This file is read literally — it does NOT read .env, so edit by hand.

PORT: 8080
HTTP_WORKERS: 2

# Public URLs served by Caddy (see Caddyfile).
PROXY_PART: https://proxy.<your-domain>
API_URL: https://api.<your-domain>
FRONTEND_URL: https://<your-domain>

# Basic hardening.
COMPROMISED_PASSWORD_CHECK: true
DISABLE_REGISTRATION: true
FEED_RETENTION: 30

# Database (matches docker-compose service + .env values).
hibernate.connection.url: jdbc:postgresql://postgres:5432/piped
hibernate.connection.username: piped
hibernate.connection.password: change_me_to_a_strong_password
hibernate.connection.driver_class: org.postgresql.Driver
hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
