# Secrets — never commit a real .env (the .env.example template is fine)
.env

# User music library (people add their own files)
local/library/music/
!local/library/music/.gitkeep

# Python caches
__pycache__/
*.pyc
